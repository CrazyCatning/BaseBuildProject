const reducer = (state = {
    lastOperand: "",
    currentOperand: "",
    operation: ""
}, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default reducer;